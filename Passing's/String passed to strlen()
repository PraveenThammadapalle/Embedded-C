#include <stdio.h>
#include <string.h>

int main() {
    char str[6] = {'H', 'e', 'l', 'l', 'o', '\0'};  // Proper null terminator

    size_t length = strlen(str);  // Correct

    printf("The length of the string is: %zu\n", length);

    return 0;
}
