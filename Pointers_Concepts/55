//basic but very important pointer-to-pointer concept

#include <stdio.h>
        void main()
        {

            int k = 5;
            int *p = &k;
            int **m  = &p;
            **m = 10;
            printf("%d\n %d\n %d\n", k, *p, **m);
        }
