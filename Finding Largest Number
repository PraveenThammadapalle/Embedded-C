#include <stdio.h>
#include <stdint.h>

#define MAX(X,Y,Z)  ((X>Y)?((X>Z)?X:Z):(Y>Z)?Y:Z)
int main()
{
    printf("MAX = %d", MAX(20,11,15) );
    
    return 0;

}


/*Explanation:
First, it checks if X > Y. If true, then it checks if X > Z. If both conditions are true, X is the largest.
If X > Y is false, it checks Y > Z. If true, it returns Y. Otherwise, it returns Z.   */
